# fix-cake-button-tracking

Anchor cake layer buttons to the cake image's bounding rect via JS, replacing viewport-relative vh positioning that fails on real devices (iOS Safari portrait/landscape, Android Chrome) due to system bars and URL bar collapse.
